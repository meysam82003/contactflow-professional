# آموزش PWA — ContactFlow Professional

## PWA چیست؟

نسخه وب ContactFlow می‌تواند از یک آدرس HTTPS باز شود و روی دستگاه‌های پشتیبانی‌شده مثل یک برنامه مستقل نصب شود. پس از اولین Cache، هسته Local مانند Import، نام‌گذاری، جستجو و Export می‌تواند بدون اتصال دائمی به Cloud کار کند؛ Sync و Telegram به اینترنت نیاز دارند.

## نصب روی Windows / macOS / Linux

1. لینک HTTPS ContactFlow را با Chrome یا Edge باز کنید.
2. گزینه Install App / Install page as app را انتخاب کنید.
3. برنامه بعد از نصب در Start Menu / Applications در دسترس است.

## نصب روی Android

1. لینک را در Chrome باز کنید.
2. Add to Home Screen / Install App را بزنید.
3. آیکون ContactFlow روی صفحه اصلی ایجاد می‌شود.

## نصب روی iPhone / iPad

1. لینک را در Safari باز کنید.
2. Share → Add to Home Screen.
3. Open as Web App را فعال کنید و Add را بزنید.

## حساب و Sync

روی دستگاه اول:

1. «حساب و Sync» را باز کنید.
2. Cloud URL را وارد کنید.
3. نام کاربری انگلیسی و رمز اصلی حداقل 12 کاراکتر وارد کنید.
4. «ساخت حساب» را بزنید.
5. «ارسال Snapshot رمزگذاری‌شده» را اجرا کنید.

روی دستگاه دوم:

1. همان Cloud URL و نام کاربری/رمز را وارد کنید.
2. «ورود» را بزنید.
3. «دریافت آخرین Snapshot» را بزنید.

Restore قبل از جایگزینی داده محلی تأیید می‌گیرد.

## Backup آفلاین

در «حساب و Sync» → «ساخت فایل .cfbackup». فایل با رمز اصلی شما رمزگذاری می‌شود. برای Restore همان رمز لازم است. رمز اشتباه قبل از پاک شدن دیتابیس محلی تشخیص داده می‌شود.

## فایل‌های یک‌میلیونی

برای Import و تبدیل اولیه دیتاست‌های بسیار بزرگ، Windows Desktop یا لپ‌تاپ مناسب‌تر است. PWA موبایل برای مدیریت، جستجو، Sync و عملیات سبک‌تر طراحی شده است.
